/** 소속 목록 조회용 공통코드 그룹 ID (`code_group.code_group_id`) */
export const AFFILIATION_CODE_GROUP_ID = "A0004";

/** 대시보드 메인 히어로 고정 소속 코드 값 (감컴퍼니 중분류 `code_detail` 코드값 등) */
export const CORE_COMPANY_AFFILIATION_CODE = "A00041";

/** `profileRowsJson`에 구조화된 행 배열을 저장할 때 붙는 접두사 */
export const PROFILE_FIELDS_JSON_PREFIX = "__PROFILE_FIELDS_JSON__:";
